# Example: AI Document Summarizer

Expected: one question at a time, Chinese UI by default for Chinese-facing users, polished UI selection, responsibility-complete architecture, no mock summary, real LLM integration, stage run checks.
