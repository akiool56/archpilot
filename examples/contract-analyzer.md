# Example: AI Contract Analyzer

Expected: sensitive data warning, no full contract logs, server-side API key only, ContractPromptBuilder separated from AIClient, RiskResultParser separated from ResultPanel, ExportService reserved for later Word memo export, no fake risk point output.
